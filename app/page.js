export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif", maxWidth: 640 }}>
      <h1>Gerador de Anúncios</h1>
      <p>
        Passo 2 concluído: stack Next.js e rota <code>POST /api/generate</code>. O formulário
        entra no Passo 3.
      </p>
      <p>
        Teste a API com <code>POST /api/generate</code> e corpo JSON — ver <code>TECH.md</code>.
      </p>
    </main>
  );
}
